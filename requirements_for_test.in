# Instead of this, we explicitly list `setup.py` in the pip-compile command as this
# line will cause notifications_utils itself to be included in the compiled requirements,
# which is not something we want.
# -e .

-r requirements_for_test_common.in

celery==5.3.6
redis>=4.3.4  # Earlier versions of redis miss features the tests need
